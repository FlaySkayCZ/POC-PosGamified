{
  "name": "accbook",
  "version": "0.0.1",
  "scripts": {
    "ng": "ng",
    "startFrontend": "npm run host-frontend --prefix ./frontend",
    "startBackend": "npm run host-backend --prefix ./backend",
    "dev": "npm run start:dev --prefix ./backend",
    "build": "ng build",
    "watch": "ng build --watch --configuration development",
    "test": "ng test"
  },
  "dependencies": {
    "express": "^4.18.2"
  }
}
